package api

import sangria.schema._

object GraphQLSchema {
  def build(): Schema[FlashbotCtx, Unit] = {
//    val query = ObjectType("Query")
//    val mutation = ObjectType("Mutation")
//    Schema(query, mutation)
    ???
  }
}
